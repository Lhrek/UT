{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build_UT",
            "type": "shell",
            "command": "cd build;cmake .. -DCMAKE_BUILD_TYPE=Debug && make -j4  ", //VERBOSE=1
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}